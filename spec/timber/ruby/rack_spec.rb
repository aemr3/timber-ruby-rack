RSpec.describe Timber::Integrations::Rack do
  it "has a version number" do
    expect(Timber::Integrations::Rack::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
